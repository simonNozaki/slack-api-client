{
  "name": "slack-client",
  "version": "1.0.0",
  "description": "client for slack",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "snozaki",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "request": "^2.88.0",
    "script-util": "^1.0.1"
  }
}
